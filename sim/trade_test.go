package sim

/*
func TestNewTradeSimulation(t *testing.T) {
	mkt := NewMatrix(1, 2)
	mkt.array[0] = 10.0
	mkt.array[1] = 20.0
	ts := NewTradeSimulation("id", 3.0, mkt)
	assert.Equal(t, ts.Id, "id", "Id should be id")
	assert.Equal(t, ts.Mtm, float32(3.0), "The mtm should be 3.0")
	tr, tc := ts.m.Dims()
	assert.Equal(t, tr, 1, "1 row")
	assert.Equal(t, tc, 2, "2 cols")
	assert.Equal(t, ts.m.array[0], float32(30.0), "1st element should be 30")
	assert.Equal(t, ts.m.array[1], float32(60.0), "2nd element should be 60")
}
*/
